foreach(eg basic_budget plan_economics hard_budget_rejection retry_cost stale_plan_revalidation post_action_verification)
  add_executable(example_${eg} example_${eg}.cpp)
  target_link_libraries(example_${eg} PRIVATE CostGovernor::CostGovernor)
  if(MSVC)
    target_compile_options(example_${eg} PRIVATE /W4 /WX /utf-8)
  else()
    target_compile_options(example_${eg} PRIVATE -Wall -Wextra -Werror -Wpedantic)
  endif()
endforeach()
